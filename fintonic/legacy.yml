# Legacy-compatibility metadata for Fintonic.
#
# Loaded by lib/freentonic/providers/legacy_keys_loader.rb. See ing/legacy.yml
# for the full comment on the security posture.
#
# Placeholders populated by the normalizer call site:
#   LegacyKeys.account(institution: :fintonic, bank_id:, product_id:, product_type:)
#   LegacyKeys.transaction(institution: :fintonic, tx_id:)

account:
  external_id: "fintonic:%{bank_id}:%{product_id}"
  uids:        ["fintonic-%{bank_id}-%{product_id}-%{product_type}"]
  bank_key:    "fintonic_%{bank_id}"

transaction:
  dedup_key: "fintonic:%{tx_id}"
