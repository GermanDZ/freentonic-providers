# Legacy-compatibility metadata for Unicaja.
#
# Loaded by lib/freentonic/providers/legacy_keys_loader.rb. See ing/legacy.yml
# for the full comment on the security posture.
#
# Placeholders populated by the normalizer call site:
#   LegacyKeys.account(institution: :unicaja, kind: "cuenta"|"tarjeta"|"prestamo", ppp:)
#   LegacyKeys.transaction(institution: :unicaja, ppp:, mv_id:)

account:
  external_id: "unicaja_live:%{kind}:%{ppp}"
  uids:        ["unicaja_live:%{kind}:%{ppp}"]
  bank_key:
    default:     "unicaja"
    if_tarjeta:  "unicaja_cc"
    if_prestamo: "unicaja_loan"

transaction:
  dedup_key: "unicaja_live:%{ppp}:%{mv_id}"
